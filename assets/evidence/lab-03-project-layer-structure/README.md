# lab-03-project-layer-structure — evidence

Proves: the layer folder skeleton (`App/Hardware/Keyboard/Usb/tools/docs/assets`) plus include paths build OK.
No logic changes yet.

## Files
- `cubeide-include-paths.png` — CubeIDE Properties for App > C/C++ Build > Settings > Include paths:
  `App`, `Hardware`, `Keyboard`, `Usb` all added to the GCC Assembler/Compiler include paths.
- `app-folder-source-entries.png`, `hardware-folder-source-entries.png`, `keyboard-folder-source-entries.png`,
  `usb-folder-source-entries.png` — each confirms one layer folder was correctly added to the CubeIDE project's source entries.
